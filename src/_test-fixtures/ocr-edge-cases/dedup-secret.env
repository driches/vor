# Intentional dedup fixture: this .env carries the SAME credential value
# encoded in dedup-secret-image.png so the test exercises whether findings
# from the text secrets scanner and the image-ocr scanner are merged or
# surfaced independently. Not a real secret.
GH_TOKEN=ghp_AbCdEfGhIjKlMnOpQrStUvWxYz0123456789
