# `_test-fixtures/webhook/`

Synthetic webhook-receiver MVP used to exercise the code-review
action against a realistic-looking small feature with mixed-category
issues (security + correctness + performance + dependency-CVE).
Do not import these from production code.

See the open PR description for the expected findings.
