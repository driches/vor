/**
 * Action entrypoint. Stub for now — Phase 6 fills this in.
 */
async function main(): Promise<void> {
  console.log('driches/code-review — Action entrypoint (scaffold)');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
