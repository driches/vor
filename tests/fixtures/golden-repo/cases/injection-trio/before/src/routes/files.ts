import * as fs from 'node:fs/promises';
import * as path from 'node:path';

const baseDir = '/srv/public';

export async function serveFile(filename: string): Promise<string> {
  // file lookup goes here
  return '';
}
